# Symbigram API credentials - TEMPLATE. This file IS committed; copy it to credentials.cfg
# (which is gitignored) and replace the dummy values with your own from
# https://my.telegram.org -> API development tools. qmake turns credentials.cfg into
# src/core/tgcredentials.h (also gitignored) through tools/gen-credentials.py.
# Format: KEY=VALUE, one per line; '#' starts a comment.
TG_API_ID=0
TG_API_HASH=0123456789abcdef0123456789abcdef
